services:
  frontend:
    build:
      context: './frontend'
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    networks:
      - "eog_resources_network"
    volumes:
      - ./frontend/dist:/usr/share/nginx/html

networks:
  eog_resources_network: